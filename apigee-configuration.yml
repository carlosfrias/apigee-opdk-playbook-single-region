---
- name: Update apigee-pre-req for the planet
  hosts: "{{ target_hosts }}"
  gather_facts: no
  serial: 1
  tags: ['apigee-config']
  roles:
  - { role: apigee-opdk-setup-silent-installation-config }
