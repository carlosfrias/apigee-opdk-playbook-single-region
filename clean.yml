---
- hosts: localhost
  connection: local
  gather_facts: no
  vars:
    clean_dirs:
    - '~/.apigee/logs_configs'
    - '~/.ansible/roles'
    - '~/.ansible/tmp'
  vars_files:
  - ~/.apigee-secure/credentials.yml
  - ~/.apigee/custom-properties.yml
  tasks:
  - name: Clean folders
    ignore_errors: yes
    file:
      path: '{{ item }}'
      state: absent
    with_items: "{{ clean_dirs }}"

  - name: Reset folders
    ignore_errors: yes
    file:
      path: '{{ item }}'
      state: directory
    with_items: "{{ clean_dirs }}"


